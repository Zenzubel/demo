<!DOCTYPE html>
<html data-theme="light" class="page page--{{ title }}" {{#if isDev }} data-dev {{/if}}>

<head>
	{{> layouts/head }}

	<title>{{ title }}</title>
</head>

<body>
	<div class="content">
		{{> layouts/header modificate="border"}}

		<main>
			{{> components/blur }}
			{{> sections/top-slider }}
			{{> sections/cards }}
			{{> sections/manufacturers titleText="Популярные производители товаров" button="true"}}
			{{> components/prefooter }}
		</main>

		{{> layouts/footer }}
	</div>

	{{> layouts/modals }}
	{{> layouts/tooltips }}
</body>

</html>